{
    "id": "planning-local",
    "description": "Local planning/spec LLM agent. Hardware-aware container, agent-owned planning profile.",
    "container": "${PLOINKY_PLANNING_LOCAL_IMAGE}",
    "llmRuntime": {
        "enabled": true
    },
    "profiles": {
        "default": {
            "openPorts": ["127.0.0.1:0:9000", "127.0.0.1:0:8080"]
        }
    },
    "readiness": {
        "protocol": "mcp"
    },
    "endpoints": {
        "agent-card": {
            "tags": ["llm", "local", "planning"]
        },
        "agentCard": {
            "tags": ["llm", "local", "planning"]
        }
    }
}
