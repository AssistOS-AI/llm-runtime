{
    "schemaVersion": 1,
    "profiles": [
        {
            "id": "primary",
            "description": "Primary chat profile. Real CPU llama.cpp launcher; fake launcher kept last for tests.",
            "candidates": [
                { "launcher": "llama-cpp-cpu", "priority": 100, "requiredAccelerators": ["cpu"] },
                { "launcher": "fake-cpu", "priority": 10, "requiredAccelerators": ["cpu"] }
            ]
        }
    ]
}
